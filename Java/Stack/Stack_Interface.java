public interface Stack_Interface<Integer> {
	boolean isEmpty();		// Returning a boolean is stack empty or not.
	void push(int item);	// Adding item to top.
	int pop();				// We are deleting top and returning top.
	int top();				// We are returning top.
}
